# 配置目录

此目录用于放置公开可提交的配置模板。
仓库展示层默认以中文说明为主，英文文档作为补充入口。

当前包含：

- `local_chat.env.example`：环境变量配置示例
- `providers.local.example.json`：多提供商本地配置模板

本地真实配置建议自行创建：

- `config/providers.local.json`

该文件已被 `.gitignore` 忽略，不应直接提交到公开仓库。
